import 'package:flutter/material.dart';
class RatingTabPage extends StatelessWidget {
  //const ProfileTabPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child:
      Text("This is Rating Tab Page"),
    );
    //return Container();
  }
}